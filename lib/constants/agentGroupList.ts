/** 그룹 목록 SSR prefetch·클라이언트 `useAgentGroupListQuery`가 동일해야 queryKey가 일치합니다. */
export const AGENT_GROUP_LIST_TAKE = 10;

/** 에이전트 생성 등 그룹 Select 드롭다운용 (한 번에 불러올 개수) */
export const AGENT_GROUP_LIST_SELECT_TAKE = 100;
